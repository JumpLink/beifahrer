{
  "name": "@beifahrer/core",
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "description": "Pure kernel shared by the bridge and the extension: the wire protocol, the per-origin policy and the redaction of tabs the agent may not see. No dependencies, no gi://, no node:*, no browser globals — every security decision in this project is unit-testable on Node and GJS.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit --project tsconfig.json",
    "lint": "gjsify lint ."
  },
  "files": [
    "src"
  ]
}
