// Kept by hand in step with package.json: a JSON import would pull the whole manifest into the
// bundle, and reading it from disk at run time breaks once the bundle is installed elsewhere.
export const VERSION = '0.1.0';
