{
  "name": "beifahrer-extension",
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "description": "The beifahrer WebExtension: connects to the local bridge and serves the agent's requests under the per-origin policy the person sets. One source, bundled by gjsify on GJS as MV3 for Chromium and MV2 for Firefox \u2014 no Node, no Vite.",
  "scripts": {
    "check": "gjsify tsc --noEmit",
    "lint": "gjsify lint .",
    "build": "gjsify build scripts/build.ts --app gjs --outfile dist/build.gjs.mjs && gjsify run dist/build.gjs.mjs",
    "zip": "gjsify build scripts/build.ts --app gjs --outfile dist/build.gjs.mjs && gjsify run dist/build.gjs.mjs --zip",
    "sign": "sh scripts/sign.sh"
  },
  "dependencies": {
    "@beifahrer/core": "*",
    "@wxt-dev/browser": "^0.3.0"
  },
  "devDependencies": {
    "@gjsify/cli": "0.52.0",
    "@gjsify/node-globals": "0.52.0",
    "@gjsify/rolldown-native": "0.52.0",
    "@gjsify/runtime": "0.52.0",
    "fflate": "^0.8.3",
    "typescript": "^6.0.3",
    "web-ext": "^10.7.0"
  }
}
